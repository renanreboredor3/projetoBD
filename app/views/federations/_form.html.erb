<%= form_for(@federation) do |f| %>
  <% if @federation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@federation.errors.count, "error") %> prohibited this federation from being saved:</h2>

      <ul>
      <% @federation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control", required: true %>
  </div>
	<div class="field">
		<%= f.label :flag %><br>
		<%= f.file_field :flag, class: "btn" %>
	</div>
  <div class="actions">
    <%= f.submit "Salvar", class: "btn btn-raised" %>
  </div>
<% end %>
