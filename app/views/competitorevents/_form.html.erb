<%= form_for(@competitorevent) do |f| %>
  <% if @competitorevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competitorevent.errors.count, "error") %> prohibited this competitorevent from being saved:</h2>

      <ul>
      <% @competitorevent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :competitor_id %><br>
    <%= f.text_field :competitor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
