<%= form_for(@gamefederation) do |f| %>
  <% if @gamefederation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gamefederation.errors.count, "error") %> prohibited this gamefederation from being saved:</h2>

      <ul>
      <% @gamefederation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :federation_id %><br>
    <%= f.text_field :federation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
